## Mood Ring

Visualize llm emotions in real time

## Quickstart

install dependencies

```bash
uv sync
```

run server

```bash
cd deploy/app
uv run server.py
```

Frontend at localhost:8080

## Batched evals

```bash
cd batch
uv run batched_eval.py
```